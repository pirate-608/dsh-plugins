[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

[project]
name = "lazy-core"
version = "0.2.6"
description = "Isolated LAZY v0.2.6 RSA compatibility subset"
requires-python = ">=3.10"
license = { text = "LGPL-3.0-only" }

[tool.hatch.build.targets.wheel]
packages = ["lazy_core"]
