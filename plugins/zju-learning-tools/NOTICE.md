# Notices

The package's first-party code is MIT licensed. The isolated `lazy-core` component is LGPL-3.0-only and retains its license at `vendor/lazy-core/LICENSE`. Other referenced projects and exact dependency provenance are listed in `UPSTREAM.json`.
